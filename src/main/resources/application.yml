spring:
  profiles:
    active: local

---

spring:
  config:
    activate:
      on-profile: local

  datasource:
    username: studym
    password: studym
    url: jdbc:mysql://localhost:5000/study_marathon

  jpa:
    open-in-view: false
